var accountView = function (userData) {
	var result = {}
	result.account="account" //account not empty to fulfill "if" condition in accountTemplate in order to display labels for an existing account
	return result
}
